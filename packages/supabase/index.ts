/**
 * @xinuco/supabase — Fase 2
 * Aquí se moverá el factory createClientFor(vertical) y los helpers
 * de cliente/servidor/middleware de Supabase.
 *
 * Por ahora cada app lleva su propia copia en apps/<app>/lib/supabase/.
 */
export {}
