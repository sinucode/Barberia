/**
 * @xinuco/config — Fase 2
 * Aquí se moverán los presets compartidos:
 * tailwind.config base, tsconfig base, eslint config, security headers CSP…
 *
 * Por ahora cada app lleva su propia copia de estos archivos.
 */
export {}
